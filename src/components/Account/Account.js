import React, { Component } from "react";
import { Switch, Route } from "react-router-dom";

const Account = (props) => {
  return (
    <div>
        <h1>test</h1>
    </div>
  );
};

export default Account;
